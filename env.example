# Variáveis de ambiente da AIFLUENT LP.
# O formulário envia os leads via FormSubmit (https://formsubmit.co) para o
# e-mail definido em src/lib/site.ts (site.contact.email) — não requer chave.
# Mantenha este arquivo apenas como referência; não há segredos obrigatórios.
